<resources xmlns:android="http://schemas.android.com/apk/res/android">



    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowBackground">@drawable/start_img</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="SplashTheme" parent="android:Theme.NoTitleBar.Fullscreen">

        <!-- 闪屏背景，可用图片 -->
        <item name="android:windowBackground">@drawable/start_img</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="bottom_tab_ll" parent="ll_vertical_match_match">
        <item name="android:width">0dp</item>
    </style>
    <style name="bottom_tab_iv" parent="img_btn">
        <item name="android:src">@drawable/dingwei</item>
    </style>
    <style name="bottom_tab_tv" parent="text_micro">
        <item name="android:textColor">@color/black_to_gray</item>
    </style>
    <style name="topbar_left_btn_dingwei" parent="topbar_btn">
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">-1dp</item>
        <item name="android:text"></item>
        <item name="android:textColor">@color/white</item>
        <item name="android:onClick">onReturnClick</item>
    </style>
    <style name="topbar_left_btn_dingwei2" parent="topbar_btn">
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">-1dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:text"></item>
        <item name="android:textColor">@color/black</item>
        <item name="android:onClick">onReturnClick</item>
    </style>
    <style name="content_item_alpha_gonggao">
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:paddingLeft">3dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@color/alpha_black</item>
    </style>
    <style name="lv2">
        <item name="android:listSelector">@drawable/to_alpha</item>
        <item name="android:divider">@color/alpha_3</item>
        <item name="android:dividerHeight">1px</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="btn_biga_round" parent="text_big">
        <item name="android:background">@drawable/btn_big_selector</item>
        <item name="android:layout_height">56dp</item>
    </style>
    <style name="btn_biga_round2" parent="text_big">
        <item name="android:background">@drawable/btn_big_selector2</item>
        <item name="android:layout_height">56dp</item>
    </style>
</resources>